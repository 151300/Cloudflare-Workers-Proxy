{
  "name": "cloudflare-workers-proxy",
  "version": "1.0.0",
  "description": "A simple and powerful reverse proxy service using Cloudflare Workers",
  "main": "worker.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.52.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
